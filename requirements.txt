# ============================================================
# Tender Analyzer Pipeline — зависимости Python
# Совместимость: Python 3.8 – 3.11
# ============================================================
 
# --- Сетевые запросы и асинхронность ---
aiohttp>=3.9.0
aiofiles>=23.2.0
 
# --- Браузерная автоматизация ---
playwright>=1.40.0
playwright-stealth>=1.0.0          # антибот-маскировка для Playwright
 
# --- HTML / XML парсинг ---
beautifulsoup4>=4.12.0
lxml>=4.9.0
 
# --- Работа с данными ---
pandas>=1.5.0
numpy>=1.24.0
 
# --- Excel ---
openpyxl>=3.1.0
xlrd>=2.0.1                        # чтение старых .xls файлов (pandas backend)
 
# --- PDF ---
pymupdf==1.24.14                # PyMuPDF (fitz) — быстрое извлечение текста
pdfplumber>=0.10.0                 # извлечение таблиц из PDF (опционально, медленнее)
 
# --- Word / DOCX ---
python-docx>=0.8.11
 
# --- Архивы ---
rarfile>=4.1                       # распаковка .rar (требует unrar в системе)
py7zr>=0.20.0                      # распаковка .7z
 
# --- Эмбеддинги и векторный поиск ---
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4                   # FAISS для RAG-классификатора (CPU версия)
                                   # для GPU: faiss-gpu>=1.7.4 (требует CUDA)
 
# --- Векторная БД (ChromaDB) для deep RAG ---
chromadb>=0.4.0
 
# --- LangChain (разбиение текста на чанки) ---
langchain>=0.1.0
langchain-text-splitters>=0.0.1   # RecursiveCharacterTextSplitter
 
# --- BM25 (гибридный поиск) ---
rank-bm25>=0.2.0
 
# --- NLP / стемминг ---
nltk>=3.8
 
# --- Прогресс-бар ---
tqdm>=4.65.0
 
# --- ML-фреймворк (эмбеддинги, device autodetect) ---
torch>=2.0.0                       # CPU-версия; для CUDA установите отдельно:
                                   # pip install torch --index-url https://download.pytorch.org/whl/cu121
scikit-learn>=1.3.0
 
# --- Переменные окружения ---
python-dotenv>=1.0.0
 
# --- Email ---
# imaplib, smtplib, email — входят в стандартную библиотеку Python
 
# ============================================================
# Платформо-зависимые зависимости
# ============================================================
 
# Обработка старых .doc файлов через MS Word COM (только Windows)
pywin32>=306; sys_platform == 'win32'
 
# ============================================================
# Опциональные зависимости
# ============================================================
 
# GPU-ускорение (CUDA) — раскомментируйте и установите вручную:
# torch>=2.0.0+cu121  # через --index-url https://download.pytorch.org/whl/cu121
 
# Ускоритель для sentence-transformers на GPU:
# accelerate>=0.20.0
